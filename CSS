<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CSS Only Portfolio</title>
  <style>
    /* Body background animation */
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      text-align: center;
      animation: bgChange 8s infinite alternate;
    }

    @keyframes bgChange {
      0%   { background-color: #f9f9f9; }
      25%  { background-color: #ffeaa7; }
      50%  { background-color: #fab1a0; }
      75%  { background-color: #a29bfe; }
      100% { background-color: #55efc4; }
    }

    header {
      background: rgba(0,0,0,0.8);
      color: white;
      padding: 20px;
    }

    h1 {
      margin: 0;
    }

    section {
      padding: 30px;
    }

    .card {
      background: white;
      width: 250px;
      margin: 20px auto;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.2);
      transition: transform 0.5s ease, background 0.5s ease;
    }

    .card:hover {
      transform: scale(1.05);
      background: #f0f8ff;
    }

    .btn {
      display: inline-block;
      background: #007bff;
      color: white;
      padding: 10px 20px;
      margin-top: 20px;
      border-radius: 5px;
      transition: background 0.5s ease;
      text-decoration: none;
    }

    .btn:hover {
      background: #0056b3;
    }
  </style>
</head>
<body>
  <header>
    <h1>My Portfolio</h1>
    <p>CSS Only - Animated Background</p>
  </header>

  <section>
    <div class="card">
      <h2>About Me</h2>
      <p>I am learning web development with HTML & CSS only.</p>
    </div>

    <div class="card">
      <h2>Projects</h2>
      <p>✔ Portfolio Website<br>✔ Calculator<br>✔ To-do App</p>
    </div>

    <a href="#" class="btn">Contact Me</a>
  </section>
</body>
</html>
